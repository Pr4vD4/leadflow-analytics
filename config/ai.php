<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Ollama API Settings
    |--------------------------------------------------------------------------
    |
    | Настройки для работы с Ollama API
    |
    */

    // URL API Ollama
    'ollama_url' => env('OLLAMA_API_URL', 'http://localhost:11434'),

    // Таймаут запросов к API в секундах
    'request_timeout' => env('OLLAMA_REQUEST_TIMEOUT', 30),

    // Модель по умолчанию для генерации текста
    'default_model' => env('OLLAMA_DEFAULT_MODEL', 'qwen2.5:1.5b'),

    // Модель по умолчанию для анализа описаний компаний
    'company_description_model' => env('OLLAMA_COMPANY_DESCRIPTION_MODEL', 'qwen2.5:3b'),

    // Модель для эмбеддингов (векторных представлений)
    'embedding_model' => env('OLLAMA_EMBEDDING_MODEL', 'bge-m3'),

    /*
    |--------------------------------------------------------------------------
    | Настройки анализа и оценки
    |--------------------------------------------------------------------------
    |
    | Настройки для анализа и оценки контента
    |
    */

    // Время кэширования результатов анализа (в минутах)
    'cache_duration' => env('AI_CACHE_DURATION', 60),

    // Минимальная длина текста для анализа
    'min_text_length' => env('AI_MIN_TEXT_LENGTH', 10),

    // Максимальная длина текста для анализа (ограничение для экономии токенов)
    'max_text_length' => env('AI_MAX_TEXT_LENGTH', 2000),

    /*
    |--------------------------------------------------------------------------
    | Очереди и асинхронная обработка
    |--------------------------------------------------------------------------
    |
    | Настройки для асинхронной обработки запросов к AI
    |
    */

    // Название очереди для задач AI
    'queue_name' => env('AI_QUEUE_NAME', 'ai-processing'),

    // Приоритет задач (более низкое значение = более высокий приоритет)
    'queue_priority' => env('AI_QUEUE_PRIORITY', 10),

];
